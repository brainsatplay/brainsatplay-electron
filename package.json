{
    "main": "brainsatplay-monorepo",
    "private": true,
    "workspaces": [
        "desktop",
        "mobile"
    ],
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "tauri": "cd build/desktop && tauri",
        "build:desktop": "CI=true npm run tauri build",
        "dev:desktop": "npm run tauri dev",
        "build:mobile": "cd mobile && npm run build",
        "dev:mobile": "ECHO 'Not yet implemented'"
      },
      "dependencies": {
        "@tauri-apps/api": "^1.2.0"
      },
      "devDependencies": {
        "@tauri-apps/cli": "^1.2.2",
        "vite": "^4.0.0",
        "typescript": "^4.8.2"
      }
}